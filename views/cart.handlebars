<div class="cartScroll">
  ${{data.cart.totalPrice}}
  <ul>
    {{#each data.items}}
    <li>
      {{this.title}}
      <div class="allItemsImage">
        <img src={{this.image}}/>
      </div>
    </li>
    {{/each}}
  <ul>
</div>