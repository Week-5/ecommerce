const { Cart } = require('../index')

describe('Cart Object Test', () => {
    // TODO: No Cart test created yet
    // Once cart functionality is address -> will be able to maneuver the cart object
});